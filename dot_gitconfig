[user]
	name = Will Bollock
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[core]
	editor = vim
[push]
	default = current
[diff]
	algorithm = patience
	context = 5
